# Rebuild — Home + Footer + Login Redirect + Route Verification

التاريخ: 2026-04-23

## ما تم تنفيذه

### 1) إعادة بناء الصفحة الرئيسية من الصفر
- Hero معلوماتي فقط: عنوان، وصف، مؤشرات مختصرة، وأزرار CTA متمركزة.
- قسمان متوازنان مباشرة أسفل Hero:
  - لوحة دخول ذكية
  - لماذا هذه الواجهة؟
- مساحة مساعد ذكي أوضح وأقصر طوليًا.
- شبكة أسئلة جاهزة 9 أزرار موحدة المقاس ومحاذاة مركزية.
- روابط تشغيل سريعة مضغوطة بدل البطاقات الطويلة.
- الإبقاء على DynamicSections أسفل الصفحة.

### 2) إعادة بناء الفوتر
- فوتر حقيقي منخفض الارتفاع.
- متمركز مع عرض الصفحة الرئيسي.
- روابط سريعة مضغوطة.
- روابط اجتماعية مختصرة عند توفرها من Site Settings.
- الاعتماد على `siteName` و `footerText` من الإعدادات المركزية.

### 3) إصلاح login redirect handling
- `client/src/const.ts`
  - تم تعديل `getLoginUrl()` ليشفّر `redirect` داخل `state` حتى في external OAuth.
- `server/_core/oauth.ts`
  - تم فك `state` واستخراج redirect الفعلي.
  - callback لم يعد يرجع دائمًا إلى `/` بعد OAuth.
  - callback صار يعيد المستخدم إلى المسار المطلوب بعد الدخول.

### 4) Route verification
تم التحقق من وجود المسارات التالية في الراوتر العام:
- `/chat`
- `/knowledge`
- `/search`
- `/references`
- `/faqs`

كما تم التحقق من وجود مسار الإدارة:
- `/admin/dashboard`

### 5) ربط الأزرار والمسارات مباشرة
الروابط الحرجة في الصفحة والفوتر أصبحت تستخدم `href` مباشرًا بدل الاعتماد على سلوك تنقل غير واضح:
- ابدأ المحادثة الكاملة
- تسجيل الدخول
- روابط التشغيل السريع
- روابط الفوتر

## الملفات المعدلة
- `client/src/pages/Home.tsx`
- `client/src/components/Footer.tsx`
- `client/src/const.ts`
- `server/_core/oauth.ts`
- `client/src/index.css`

## ملاحظة
هذا Rebuild نظيف للصفحة الرئيسية والفوتر ومسار تسجيل الدخول/العودة، مع اعتماد classes جديدة مستقلة (`rebuild-*`) لتجنب التضارب مع طبقات التعديلات السابقة.
