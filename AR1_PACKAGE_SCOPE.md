# AR1 Package Scope

`MEGA_BATCH_AR1 — GOVERNED_AGENTIC_RAG_INTERNAL_PILOT_VERTICAL_SLICE`

An admin-only internal Pilot Vertical Slice built on the applied C4 reconciliation and AR1A architecture contract.

Included: ephemeral C4-bound corpus session (max 5), scoped retrieval from existing `knowledge_document` rows, T3/T4 operator session declarations, citation IDs, evidence-only fallback, optional internally-approved LLM synthesis, legal-conclusion escalation, temporary trace, rollback, UI control surface, verifier, and UAT documentation.

Excluded: SQL, schema change, source link write, rights assignment, new document import, file copy, chunking, embeddings, vectors, public Chat, public display, public release, or production.
