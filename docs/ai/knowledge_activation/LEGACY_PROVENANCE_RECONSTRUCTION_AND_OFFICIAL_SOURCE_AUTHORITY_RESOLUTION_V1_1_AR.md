# MEGA_BATCH_ASSISTANT_LEGACY_PROVENANCE_RECONSTRUCTION_AND_OFFICIAL_SOURCE_AUTHORITY_RESOLUTION_V1_1_NESTED_EVIDENCE_AND_DETERMINISTIC_CROSSWALK

## القرار الحاكم
`binding_method` يثبت فقط مسارًا موروثًا قابلًا للتتبع. لا يثبت صحة الرابط حيًا، ولا جهة الإصدار، ولا السلطة الرسمية، ولا صلاحية Chat.

## طبقات V1.1
- exact legacy key
- exact content fingerprint
- exact title + URL
- unique title + valid URL candidate
- title only review
- manual containment

## سياسة الروابط
- `example.com` يستبعد تلقائيًا كأثر اختبار.
- نطاقات .gov.ps أو مؤسسات جامعية هي host hint فقط.
- لا مرشح يتحول إلى `official`/`verified`/`approved`/`chat eligible` تلقائيًا.
