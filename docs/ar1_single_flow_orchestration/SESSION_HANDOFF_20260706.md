# Session Handoff — MEGA_BATCH_AR1_SINGLE_FLOW_ORCHESTRATION_AND_OPERATOR_CLARITY_V1

## الحالة المعتمدة

تم قبول دفعة المسار الموحد محليًا بعد تحقق ثابت ناجح وUAT متصفح ناجح. أصبحت واجهة AR1 تعرض للمشغّل إجراء واحدًا: بدء دورة تحقق. ينفذ المسار C3 المحدود ثم C4 وفحص الأهلية الحتمية داخل العملية الخادمية نفسها.

## النتيجة التشغيلية الحالية

- فحوص C3 المنفذة: 30.
- مواد C4 القابلة للربط حتميًا: 0.
- جلسات AR1 المنشأة: 0.
- القرار: لا توجد مادة قابلة للربط حتميًا؛ لا توجد خطوة تشغيلية إضافية.

هذه نتيجة حوكمة مقبولة وليست عطلًا. لا يجوز تشغيل دورات متكررة بلا تغير في بيانات C2/C3/C4، ولا يجوز إنشاء جلسة أو اختيار مادة تلقائيًا.

## ما تم تغييره

- استبدال التنقل اليدوي المربك بين C3 وC4 بزر واحد ودورة تشغيل موحدة.
- إبقاء اختيار المرشح صريحًا وبمادة واحدة فقط قبل إظهار اعتماد الجلسة.
- حفظ حدود no-write ورفض المطابقة التقريبية/الدلالية/المتجهية.
- نقل الحوكمة والتفاصيل التقنية إلى أقسام ثانوية.

## ما لم يتغير

- لا SQL ولا Schema/RLS/RPC.
- لا تفعيل لطبقة الحقوق أو سجلات الحقوق الداعمة.
- لا كتابة مصادر أو حقوق أو وثائق أو chunks أو embeddings أو vectors.
- لا Chat عام، ولا Knowledge Release، ولا Staging أو Production.

## baseline السابق والمرجع الجديد

- المرجع السابق: `PALWAKF_ASSISTANT_SOURCE_BASELINE_AR1_SOURCE_PROVENANCE_RIGHTS_OPERATIONAL_UX_REFINEMENT_V1_20260706_R1`.
- المرجع الجديد بعد تنفيذ حزمة الإغلاق: `PALWAKF_ASSISTANT_SOURCE_BASELINE_AR1_SINGLE_FLOW_ORCHESTRATION_AND_OPERATOR_CLARITY_V1_20260706_R1`.

## نقطة الاستئناف الوحيدة

لا يبدأ `AR1 controlled internal pilot` إلا بعد تحقق جميع الشروط التالية:

1. تظهر مادة C4 واحدة على الأقل عبر مرجع C4 مباشر، أو إحالة مرجعية مباشرة فريدة، أو تطابق حتمي دقيق للعنوان/الرابط.
2. لا يوجد غموض أو تعدد غير محسوم في الوثائق المرتبطة.
3. يختار المشغّل مادة واحدة صراحةً.
4. تتوفر مراجع استخدام داخلي وحقوق ومراجع T3/T4 المطلوبة على مستوى الجلسة.

بعد ذلك فقط: `operator selects one candidate → AR1 controlled internal pilot`، وبجلسة داخلية مؤقتة، دون نشر أو إطلاق معرفة.
