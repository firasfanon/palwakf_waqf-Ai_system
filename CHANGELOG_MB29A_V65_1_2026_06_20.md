# Changelog — MB29A v65.1

## Added
- Guarded Windows PowerShell source patch script.
- Error record and Arabic execution guide.

## Changed
- `server/rag.ts`: resolves `authorityLevel` via governed trust assessment rather than an undefined field on `KnowledgeDocument`.

## Not changed
- No database objects, SQL policies, RPC contracts, runtime route privileges, browser UI, or deployment configuration.
