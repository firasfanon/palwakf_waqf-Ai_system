# Big Batch — Assistant & Smart Tools Testing

التاريخ: 2026-04-23

## هدف الدفعة
تحويل صفحة الشات من مجرد واجهة محادثة إلى مساحة اختبار فعلية للمساعد الذكي والأدوات الذكية، مع تثبيت مسارات التنقل الفعلية في بيئة Hash Routing.

## ما تم تنفيذه

### 1) Hash-aware routing foundation
- إضافة helper مركزي جديد في `client/src/const.ts`:
  - `isHashRoutingEnabled()`
  - `getAppHref(path)`
  - `getAppRedirectTarget(path)`
- جعل `getLoginUrl()` واعيًا لـ Hash Routing حتى يحفظ redirect الصحيح مثل:
  - `/knowledge#/chat`
  - وليس فقط `/chat`

### 2) تحسين ربط الصفحة الرئيسية بالشات
- تحديث `Home.tsx` لاستخدام `getAppHref()` في الروابط المباشرة إلى الصفحات العامة.
- الحفاظ على `getLoginUrl()` للمسارات التي تحتاج redirect بعد تسجيل الدخول.

### 3) تطوير كبير في صفحة الشات نفسها
تم تحويل `client/src/pages/Chat.tsx` إلى مساحة اختبار فعلية تشمل:

#### أ) تشغيل المحادثة بدون خطوة يدوية زائدة
- لم يعد إرسال الرسالة يعتمد على إنشاء محادثة يدويًا أولًا.
- عند أول إرسال، يتم إنشاء المحادثة تلقائيًا ثم إرسال الرسالة.

#### ب) سيناريوهات اختبار جاهزة
- إضافة 6 Playbooks اختبارية جاهزة:
  - قانوني
  - فقهي
  - بحثي
  - إداري
  - قراءة وثيقة
  - تاريخي
- كل Playbook يمكن تشغيله مباشرة لبدء اختبار فعلي للنموذج.

#### ج) مختبر الأدوات الذكية
إضافة Smart Tools Lab داخل صفحة الشات يتضمن:
- أداة التلخيص
- أداة الاستخراج
- أداة التصنيف
- إمكانية إدخال عنوان اختياري للنص
- إمكانية لصق نص فعلي وتجربة الأدوات عليه مباشرة
- عرض الناتج داخل الصفحة
- زر لاستخدام الناتج داخل المحادثة
- زر لحفظ الناتج كمسودة معرفة

#### د) توسيع الوصول إلى أدوات الذكاء للاختبار
في `server/routers.ts`:
- تم تحويل هذه الأدوات من `adminProcedure` إلى `protectedProcedure` لتسهيل الاختبار الفعلي للمستخدم المصادق:
  - `aiTools.classify`
  - `aiTools.extract`
  - `aiTools.summarize`
- بينما بقي حفظ الناتج كمسودة معرفة (`saveAsKnowledgeDraft`) إداريًا فقط.

#### هـ) التحقق من المسارات داخل صفحة الشات
إضافة Route Verification panel يعرض الروابط الفعلية التالية بطريقة واضحة وقابلة للنقر:
- chat
- knowledge
- search
- faqs

#### و) تحسين تجربة الاختبار أثناء المحادثة
- إضافة 4 أزرار prompts سريعة فوق composer أثناء وجود محادثة نشطة.
- عند النقر عليها يتم تعبئة السؤال بسرعة لاختبار النموذج في نفس الجلسة.

## الملفات المعدلة
- `client/src/const.ts`
- `client/src/pages/Home.tsx`
- `client/src/pages/Chat.tsx`
- `server/routers.ts`

## النتيجة العملية
النسخة أصبحت أقرب إلى:
- منصة اختبار فعلية للنموذج
- وليس مجرد واجهة دردشة

وتدعم الآن:
- اختبار محادثة مباشر
- اختبار أدوات التلخيص/الاستخراج/التصنيف
- التحقق من المسارات الفعلية
- توافق أفضل مع Hash Routing المعتمد فعليًا في المشروع
