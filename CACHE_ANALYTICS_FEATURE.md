# صفحة إحصائيات Cache (Cache Analytics Dashboard)

## نظرة عامة

تم إضافة صفحة شاملة لمراقبة وإدارة نظام Cache للإجابات المتكررة في لوحة التحكم. توفر هذه الصفحة رؤية كاملة لأداء النظام والأسئلة الأكثر شيوعاً، مع أدوات لتحسين الأداء وتحديث الأسئلة المقترحة تلقائياً.

## المسار

```
/admin/cache-analytics
```

**ملاحظة:** يتطلب صلاحيات Admin للوصول.

## الميزات الرئيسية

### 1. البطاقات الإحصائية (Stats Cards)

تعرض 4 بطاقات رئيسية في أعلى الصفحة:

- **إجمالي الإجابات المحفوظة** (Total Cached): عدد الإجابات المخزنة في Cache
- **إجمالي الاستخدامات** (Total Hits): عدد المرات التي تم استخدام Cache فيها
- **معدل Cache Hit Rate**: نسبة نجاح Cache (الاستخدامات / الإجمالي × 100)
- **متوسط التقييم** (Average Rating): متوسط تقييمات المستخدمين للإجابات

### 2. الرسوم البيانية (Charts)

#### أ. معدل Cache Hit Rate (Line Chart)
- يعرض معدل الإصابة الحالي مقارنة بالمستهدف (85%)
- يساعد في تقييم كفاءة النظام

#### ب. توزيع الأسئلة حسب الفئة (Pie Chart)
- يوضح توزيع الأسئلة المحفوظة حسب الفئات:
  - قانونية (Legal)
  - فقهية (Jurisprudence)
  - إدارية (Administrative)
  - تاريخية (Historical)
  - عامة (General)

#### ج. توزيع التقييمات (Bar Chart)
- يعرض تقييمات أفضل 10 أسئلة
- يساعد في تحديد الإجابات الأكثر جودة

### 3. جدول الأسئلة الأكثر شيوعاً

يعرض أكثر 20 سؤال استخداماً مع:
- **السؤال الأصلي**: النص الكامل للسؤال
- **الفئة**: تصنيف السؤال (مع ألوان مميزة)
- **عدد الاستخدامات** (Hit Count): عدد المرات التي تم استخدام هذه الإجابة
- **التقييم** (Rating): متوسط تقييم المستخدمين

**ملاحظة:** يتم تصفية الأسئلة المنتهية الصلاحية تلقائياً.

### 4. أدوات الإدارة

#### أ. تنظيف Cache المنتهي
- **الزر**: "تنظيف المنتهي"
- **الوظيفة**: حذف جميع الإجابات المنتهية الصلاحية (expiresAt < الآن)
- **النتيجة**: يعرض عدد الإجابات المحذوفة في Toast notification

#### ب. تحديث الأسئلة المقترحة
- **الزر**: "تحديث الأسئلة المقترحة"
- **الوظيفة**: استخراج أكثر 12 سؤال شيوعاً لاستخدامها كأسئلة مقترحة
- **النتيجة**: يعرض معاينة (Preview Dialog) للأسئلة الجديدة مع:
  - نص السؤال
  - الفئة
  - عدد الاستخدامات
  - التقييم
- **الخطوة التالية**: يمكن نسخ هذه الأسئلة وتحديث ملف `SuggestedQuestions.tsx` يدوياً

## البنية التقنية

### Backend (tRPC Procedures)

تم إضافة 4 procedures جديدة في `server/routers.ts`:

#### 1. `cache.getStats`
```typescript
cache.getStats.useQuery()
```
- **الصلاحية**: Admin فقط
- **الإرجاع**:
  ```typescript
  {
    totalCached: number;
    totalHits: number;
    avgRating: number;
    topQuestions: Array<{
      question: string;
      hitCount: number;
    }>;
  }
  ```

#### 2. `cache.getMostFrequent`
```typescript
cache.getMostFrequent.useQuery({ limit: 20 })
```
- **الصلاحية**: Admin فقط
- **المدخلات**: `{ limit?: number }` (افتراضي: 20)
- **الإرجاع**: `CachedResponse[]` (مصفاة من المنتهي)

#### 3. `cache.cleanExpired`
```typescript
cache.cleanExpired.useMutation()
```
- **الصلاحية**: Admin فقط
- **الإرجاع**: `{ success: boolean; deletedCount: number }`

#### 4. `cache.updateSuggestedQuestions`
```typescript
cache.updateSuggestedQuestions.useMutation({ topN: 12 })
```
- **الصلاحية**: Admin فقط
- **المدخلات**: `{ topN?: number }` (افتراضي: 12)
- **الإرجاع**:
  ```typescript
  {
    success: boolean;
    suggestedQuestions: Array<{
      question: string;
      category: string;
      hitCount: number;
      rating: number;
    }>;
  }
  ```

### Frontend

#### الملفات المضافة/المعدلة:

1. **`client/src/pages/CacheAnalytics.tsx`** (جديد)
   - الصفحة الرئيسية للإحصائيات
   - يستخدم Recharts للرسوم البيانية
   - يستخدم shadcn/ui للمكونات

2. **`client/src/App.tsx`** (معدل)
   - إضافة Route: `/admin/cache-analytics`

3. **`client/src/components/DashboardLayout.tsx`** (معدل)
   - إضافة رابط "إحصائيات Cache" في Sidebar

4. **`client/src/pages/AdminDashboard.tsx`** (معدل)
   - إضافة بطاقة "إحصائيات Cache" في قسم "إدارة النظام"

### التحسينات على server/cache.ts

تم تحديث `getMostFrequentQuestions()` لتصفية الإجابات المنتهية الصلاحية:

```typescript
export async function getMostFrequentQuestions(limit: number = 20): Promise<CachedResponse[]> {
  const db = await getDb();
  if (!db) return [];

  try {
    const now = new Date();
    return await db
      .select()
      .from(cachedResponses)
      .where(
        sql`(${cachedResponses.expiresAt} IS NULL OR ${cachedResponses.expiresAt} > ${now})`
      )
      .orderBy(desc(cachedResponses.hitCount))
      .limit(limit);
  } catch (error) {
    console.error('[Cache] Error getting frequent questions:', error);
    return [];
  }
}
```

## الاختبارات (Vitest)

تم إنشاء ملف اختبار شامل: `server/cache.analytics.test.ts`

### الإحصائيات:
- **إجمالي الاختبارات**: 18
- **النتيجة**: ✅ جميعها ناجحة

### تغطية الاختبارات:

1. **cache.getStats** (3 اختبارات)
   - ✅ إرجاع الإحصائيات بشكل صحيح
   - ✅ ترتيب topQuestions حسب hitCount
   - ✅ حساب متوسط التقييم بشكل صحيح

2. **cache.getMostFrequent** (5 اختبارات)
   - ✅ إرجاع الأسئلة مع الحد الافتراضي (20)
   - ✅ إرجاع الأسئلة مع حد مخصص
   - ✅ ترتيب الأسئلة حسب hitCount تنازلياً
   - ✅ تضمين جميع الحقول المطلوبة
   - ✅ تصفية الإجابات المنتهية الصلاحية

3. **cache.cleanExpired** (2 اختبارات)
   - ✅ حذف الإجابات المنتهية
   - ✅ عدم حذف الإجابات الصالحة

4. **cache.updateSuggestedQuestions** (4 اختبارات)
   - ✅ إرجاع الأسئلة المقترحة
   - ✅ إرجاع العدد الصحيح
   - ✅ تضمين جميع الحقول المطلوبة
   - ✅ ترتيب الأسئلة حسب hitCount

5. **Authorization** (4 اختبارات)
   - ✅ منع المستخدمين غير Admin من الوصول إلى جميع الـ procedures

## الفوائد

### 1. مراقبة الأداء
- رؤية واضحة لكفاءة نظام Cache
- تحديد الأسئلة الأكثر شيوعاً
- قياس جودة الإجابات من خلال التقييمات

### 2. تحسين التجربة
- تحديث الأسئلة المقترحة بناءً على البيانات الفعلية
- توفير أسئلة أكثر صلة للمستخدمين
- تقليل الأسئلة المكررة

### 3. إدارة فعالة
- تنظيف Cache المنتهي بسهولة
- تحرير مساحة قاعدة البيانات
- الحفاظ على أداء النظام

### 4. اتخاذ قرارات مبنية على البيانات
- فهم اهتمامات المستخدمين
- تحديد الفئات الأكثر طلباً
- تحسين المحتوى بناءً على الإحصائيات

## الاستخدام الموصى به

### 1. المراقبة الدورية
- افتح الصفحة أسبوعياً لمراقبة الأداء
- تحقق من معدل Cache Hit Rate (الهدف: >85%)
- راقب متوسط التقييم (الهدف: >4.0)

### 2. التنظيف الشهري
- نظف Cache المنتهي مرة شهرياً
- حافظ على قاعدة بيانات نظيفة وسريعة

### 3. تحديث الأسئلة المقترحة
- حدث الأسئلة المقترحة كل 3-6 أشهر
- استخدم البيانات الفعلية لتحسين التجربة

### 4. تحليل الاتجاهات
- راقب توزيع الفئات لفهم اهتمامات المستخدمين
- حدد الفئات التي تحتاج إلى محتوى إضافي

## الأمان

- ✅ جميع الـ procedures تتطلب صلاحيات Admin
- ✅ التحقق من الصلاحيات على مستوى tRPC
- ✅ لا يمكن للمستخدمين العاديين الوصول إلى هذه الصفحة
- ✅ جميع العمليات مسجلة في قاعدة البيانات

## الأداء

- ⚡ استعلامات محسّنة مع Indexes
- ⚡ تصفية الإجابات المنتهية على مستوى قاعدة البيانات
- ⚡ Pagination للأسئلة الأكثر شيوعاً (limit: 20)
- ⚡ Caching على مستوى React Query (staleTime: 5 minutes)

## الخطوات المستقبلية (اختياري)

1. **رسوم بيانية متقدمة**
   - رسم بياني لتطور Cache Hit Rate عبر الزمن
   - رسم بياني لتوزيع الأسئلة حسب التاريخ

2. **تصدير التقارير**
   - تصدير الإحصائيات إلى PDF
   - تصدير الأسئلة الأكثر شيوعاً إلى Excel

3. **إشعارات تلقائية**
   - إشعار عند انخفاض Cache Hit Rate عن حد معين
   - إشعار عند امتلاء Cache

4. **تحديث تلقائي للأسئلة المقترحة**
   - تحديث ملف `SuggestedQuestions.tsx` تلقائياً
   - جدولة التحديث شهرياً

## الخلاصة

صفحة إحصائيات Cache توفر أداة قوية لمراقبة وإدارة نظام Cache في التطبيق. من خلال البيانات الواضحة والرسوم البيانية التفاعلية، يمكن للمسؤولين اتخاذ قرارات مبنية على البيانات لتحسين تجربة المستخدم وأداء النظام.

---

**تاريخ الإنشاء**: 2026-01-08  
**الإصدار**: 1.0.0  
**المطور**: Manus AI
