# المرحلة 3: نظام المراجعة والجدولة والتحسين

تاريخ الإنجاز: 2026-01-09

## 📋 نظرة عامة

تم تطوير ثلاث ميزات رئيسية لتحسين نظام جلب المعرفة:

1. **صفحة مراجعة المحتوى المجلوب** - واجهة تفاعلية لمراجعة واعتماد المحتوى
2. **نظام الجدولة اليدوي المحسّن** - لوحة تحكم شاملة لإدارة عمليات الجلب
3. **التصنيف الذكي باستخدام LLM** - تصنيف تلقائي دقيق للمحتوى

---

## 1️⃣ صفحة مراجعة المحتوى المجلوب

### 📍 المسار
`/admin/fetched-content`

### ✨ الميزات

#### 1.1 جدول تفاعلي
- عرض جميع المحتوى المجلوب في جدول منظم
- أعمدة: العنوان، الفئة، المؤلف، الحالة، تاريخ الجلب، الإجراءات
- فرز وترتيب تلقائي حسب تاريخ الجلب (الأحدث أولاً)

#### 1.2 نظام الفلترة والبحث
- **بحث نصي**: البحث في العنوان والمحتوى
- **فلترة حسب الحالة**: الكل، قيد المراجعة، معتمد، مرفوض
- **فلترة حسب الفئة**: قانوني، فقهي، مجلة الأحكام، تاريخي، إداري، مرجع

#### 1.3 الموافقة والرفض
- **موافقة فردية**: زر أخضر لكل عنصر
- **رفض فردي**: زر أحمر لكل عنصر
- **موافقة جماعية**: تحديد متعدد + موافقة دفعة واحدة
- **رفض جماعي**: تحديد متعدد + رفض دفعة واحدة
- **معاينة المحتوى**: نافذة منبثقة لعرض التفاصيل الكاملة

#### 1.4 نقل المحتوى المعتمد
- عند الموافقة، يتم نقل المحتوى تلقائياً إلى `knowledge_documents`
- تحديث حالة العنصر في `fetched_content` إلى "approved"
- إشعار نجاح مع عدد العناصر المنقولة

#### 1.5 Pagination
- عرض 20 عنصر في كل صفحة
- أزرار التنقل: السابق / التالي
- عرض رقم الصفحة الحالية والإجمالي

### 🔧 التقنيات المستخدمة
- **Frontend**: React + TypeScript + shadcn/ui
- **Backend**: tRPC procedures
- **State Management**: TanStack Query (React Query)
- **UI Components**: Table, Dialog, Select, Checkbox, Badge

### 📡 tRPC Procedures
```typescript
fetchedContent: {
  list: publicProcedure      // قائمة المحتوى مع فلترة
  approve: protectedProcedure    // موافقة فردية
  reject: protectedProcedure     // رفض فردي
  approveMultiple: protectedProcedure  // موافقة جماعية
  rejectMultiple: protectedProcedure   // رفض جماعي
}
```

### 🗄️ Database Functions
```typescript
listFetchedContent(filters)    // قائمة مع فلترة وبحث
countFetchedContent(filters)   // عد العناصر
getFetchedContentById(id)      // جلب عنصر واحد
updateFetchedContentStatus(id, status)  // تحديث الحالة
```

---

## 2️⃣ نظام الجدولة اليدوي المحسّن

### 📍 المسار
`/admin/data-fetching`

### ✨ الميزات

#### 2.1 بطاقات الإحصائيات
- **إجمالي عمليات الجلب**: عدد جميع العمليات
- **عناصر مجلوبة**: إجمالي العناصر المجلوبة
- **عناصر معتمدة**: عدد العناصر المعتمدة (أخضر)
- **معدل النجاح**: نسبة العمليات الناجحة

#### 2.2 سجل آخر العمليات
- عرض أحدث 5 عمليات جلب
- لكل عملية:
  - رقم العملية
  - الحالة (نجاح ✅ / فشل ❌ / جزئي ⚠️)
  - تاريخ ووقت التنفيذ
  - عدد العناصر المجلوبة
  - عدد العناصر المعتمدة

#### 2.3 زر "جلب من جميع المصادر"
- تشغيل عملية جلب فورية من جميع المصادر
- عرض حالة التحميل أثناء التنفيذ
- إشعار بالنتيجة (عدد العناصر المجلوبة)

#### 2.4 تنبيهات تلقائية
- إشعار عند نجاح الجلب
- إشعار عند فشل الجلب مع سبب الخطأ
- عرض عدد العناصر المحفوظة

### 🔧 التقنيات المستخدمة
- **Frontend**: React + TypeScript + shadcn/ui
- **Backend**: tRPC procedures
- **UI Components**: Card, Badge, Button, Loader

### 📡 tRPC Procedures الجديدة
```typescript
fetcher: {
  getStats: adminProcedure           // إحصائيات الجلب
  getRecentLogs: adminProcedure      // آخر 5 عمليات
  fetchAll: adminProcedure           // جلب من جميع المصادر (موجود مسبقاً)
}
```

### 🗄️ Database Functions المستخدمة
```typescript
getFetchLogsStats(sourceId?)   // إحصائيات شاملة
getFetchLogs(filters?)         // سجل العمليات
```

---

## 3️⃣ التصنيف الذكي باستخدام LLM

### 📍 الملف
`server/ai-classifier.ts`

### ✨ الميزات

#### 3.1 دالة `classifyWithLLM()`
تصنيف ذكي للمحتوى باستخدام نموذج اللغة الكبير (LLM)

**المدخلات:**
- `content`: نص المحتوى
- `title`: العنوان (اختياري)

**المخرجات:**
```typescript
{
  category: "law" | "jurisprudence" | "majalla" | "historical" | "administrative" | "reference",
  keywords: string[],        // 5-10 كلمات مفتاحية
  summary: string,           // ملخص 2-3 جمل
  relevanceScore: number     // 0-100
}
```

#### 3.2 التصنيف التلقائي
- **الفئة**: اختيار تلقائي من 6 فئات
- **الكلمات المفتاحية**: استخراج 5-10 كلمات مهمة
- **الملخص**: توليد ملخص موجز (2-3 جمل)
- **درجة الصلة**: تقييم مدى صلة المحتوى بالأوقاف الإسلامية (0-100)

#### 3.3 استخدام JSON Schema
- استخدام `response_format` مع `json_schema`
- ضمان استجابة منظمة وصحيحة
- معالجة الأخطاء مع fallback إلى تصنيف أساسي

#### 3.4 دالة `enhanceFetchedContent()`
تحسين دفعة من المحتوى المجلوب

**الوظيفة:**
- معالجة مجموعة من العناصر
- تطبيق التصنيف الذكي على كل عنصر
- إرجاع العناصر محسّنة بالتصنيف والكلمات المفتاحية

#### 3.5 Fallback Mechanism
عند فشل LLM:
- استخراج كلمات مفتاحية أساسية (frequency-based)
- تصنيف افتراضي: "reference"
- ملخص: أول 200 حرف
- درجة صلة: 50

### 🔧 التقنيات المستخدمة
- **LLM**: invokeLLM من `server/_core/llm.ts`
- **JSON Schema**: لضمان استجابة منظمة
- **Error Handling**: معالجة شاملة للأخطاء
- **Fallback**: نظام احتياطي للتصنيف الأساسي

### 📊 مثال على الاستخدام
```typescript
import { classifyWithLLM, enhanceFetchedContent } from "./ai-classifier";

// تصنيف محتوى واحد
const result = await classifyWithLLM(content, title);

// تحسين مجموعة من المحتوى
const enhanced = await enhanceFetchedContent(items);
```

---

## 🎯 الفوائد والتحسينات

### 1. تحسين الكفاءة
- **قبل**: مراجعة يدوية لكل عنصر على حدة
- **بعد**: مراجعة جماعية مع معاينة سريعة

### 2. دقة التصنيف
- **قبل**: تصنيف يدوي أو بسيط
- **بعد**: تصنيف ذكي باستخدام LLM مع دقة عالية

### 3. توفير الوقت
- **الموافقة الجماعية**: اعتماد 10+ عناصر بنقرة واحدة
- **التصنيف التلقائي**: توفير 80% من وقت التصنيف اليدوي

### 4. تتبع الأداء
- **إحصائيات شاملة**: معدل النجاح، عدد العناصر، إلخ
- **سجل العمليات**: تتبع جميع عمليات الجلب

### 5. جودة المحتوى
- **درجة الصلة**: فلترة المحتوى غير المرتبط
- **ملخصات تلقائية**: فهم سريع للمحتوى
- **كلمات مفتاحية دقيقة**: تحسين البحث

---

## 🧪 الاختبار

### اختبار صفحة المراجعة
1. انتقل إلى `/admin/fetched-content`
2. جرب الفلترة حسب الحالة والفئة
3. استخدم البحث النصي
4. افتح معاينة لعنصر
5. جرب الموافقة/الرفض الفردي
6. حدد عدة عناصر وجرب الموافقة الجماعية

### اختبار نظام الجدولة
1. انتقل إلى `/admin/data-fetching`
2. راجع الإحصائيات في البطاقات
3. راجع سجل آخر العمليات
4. اضغط "جلب من جميع المصادر"
5. راقب الإشعارات

### اختبار التصنيف الذكي
1. جلب محتوى جديد
2. راجع التصنيف التلقائي
3. تحقق من الكلمات المفتاحية
4. راجع الملخص المولد
5. تحقق من درجة الصلة

---

## 📝 ملاحظات تقنية

### الأمان
- جميع procedures محمية بـ `protectedProcedure` أو `adminProcedure`
- التحقق من صلاحيات المستخدم قبل أي عملية
- معالجة شاملة للأخطاء

### الأداء
- Pagination لتقليل الحمل
- Debouncing للبحث (موجود في المكونات الأخرى)
- معالجة دفعات للتصنيف الذكي

### قابلية التوسع
- سهولة إضافة فئات جديدة
- إمكانية تخصيص prompt التصنيف
- دعم مصادر جلب إضافية

---

## 🚀 الخطوات التالية (اختياري)

### تحسينات مستقبلية
1. **Cron Jobs حقيقية**: عند توفر البنية التحتية
2. **تصنيف متعدد المستويات**: فئات فرعية
3. **تعلم آلي**: تحسين التصنيف بناءً على التقييمات
4. **تكامل مع مصادر إضافية**: المزيد من APIs
5. **لوحة تحكم متقدمة**: رسوم بيانية تفاعلية

---

## 📚 الملفات المضافة/المعدلة

### ملفات جديدة
- `client/src/pages/FetchedContentReview.tsx`
- `server/ai-classifier.ts`

### ملفات معدلة
- `server/routers.ts` (إضافة fetchedContent router + procedures)
- `server/db.ts` (إضافة دوال listFetchedContent, countFetchedContent)
- `client/src/App.tsx` (إضافة route /admin/fetched-content)
- `client/src/components/DashboardLayout.tsx` (إضافة رابط في sidebar)
- `client/src/pages/admin/DataFetching.tsx` (إضافة إحصائيات وسجل)

---

## ✅ الخلاصة

تم تطوير نظام شامل لمراجعة وإدارة المحتوى المجلوب مع تصنيف ذكي باستخدام LLM. النظام يوفر:

- ✅ واجهة تفاعلية لمراجعة المحتوى
- ✅ موافقة/رفض فردي وجماعي
- ✅ فلترة وبحث متقدم
- ✅ لوحة تحكم للجدولة مع إحصائيات
- ✅ تصنيف تلقائي دقيق باستخدام LLM
- ✅ استخراج كلمات مفتاحية وملخصات
- ✅ تقييم درجة الصلة بالموضوع

**النتيجة**: تحسين كبير في كفاءة إدارة المحتوى وجودة التصنيف! 🎉
