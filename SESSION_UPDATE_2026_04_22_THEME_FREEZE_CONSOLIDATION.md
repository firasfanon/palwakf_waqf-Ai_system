# Theme Freeze + Central Theme Consolidation Patch

## الهدف
تجميد نظام الثيمات المتعدد مؤقتًا واعتماد ثيم مركزي واحد موحد حتى إنهاء تنظيف التنسيقات المتناثرة من الصفحات.

## ما تم
- تجميد ThemeContext على ثيم واحد `pwf-core`
- فرض `.dark` + `data-theme="pwf-core"` مبكرًا في `main.tsx`
- تبسيط `ThemeShell` في `App.tsx` ومنع الفصل الحالي admin/public للثيم
- تقليص `predefinedThemes` إلى ثيم واحد معتمد فقط
- إزالة زر تبديل الثيم من `Navbar`
- توحيد التوكنات المركزية في `index.css`
- إضافة Surface normalization أولي للنوافذ والحقول في `admin.css`

## المرحلة التالية
- تنظيف الصفحات الأكثر تشظيًا صفحة صفحة
- إزالة gradients والألوان المحلية من الصفحات التي لا تستخدم التوكنات المركزية
- إعادة بناء light/dark لاحقًا من لوحة التحكم بعد ثبات الأسطح والهوية
