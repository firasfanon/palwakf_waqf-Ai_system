# Session Handoff — Sovereign Batch 02A.1

## Apply sequence
1. Apply the package with the supplied PowerShell script.
2. Run `pnpm.cmd run check` and `pnpm.cmd run build`.
3. Run the local dev server and visit `/knowledge#/admin/knowledge-review-operations`.
4. Open exactly one citation-verification case using **فتح ملف**.
5. Capture one screenshot at desktop width and one at narrow width, validating single dialog surface, internal scroll and fixed footer.
6. Inspect terminal: an assistant companion read outage must use one bounded warning/fallback, not repeated `reference_documents/reference_files/knowledge_citations fetch failed` warnings per request.

## Do not do
- Do not claim, verify, classify, map or release a task during this retest.
- Do not run any SQL.
- Do not use `assistant.publish`.
